<%= form_with(model: visit_company, local: true) do |form| %>
  <% if visit_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit_company.errors.count, "error") %> prohibited this visit_company from being saved:</h2>

      <ul>
      <% visit_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
